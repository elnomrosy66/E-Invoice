# توثيق الثغرات الأمنية لحزم NuGet المؤجلة

يحتوي هذا الملف على تفاصيل الثغرات الأمنية المكتشفة في حزم NuGet المستهدفة والتي تم تأجيل تحديثها بناءً على رغبة المستخدم لتفادي أي تغييرات جوهرية غير مرغوبة في منطق التشفير أو معالجة الـ JSON حالياً.

---

## 1. مكتبة BouncyCastle (الإصدار الحالي: 1.8.9)
تُستخدم هذه المكتبة في توقيع الفواتير وتشفير البيانات للتكامل مع مصلحة الضرائب.

* **مستوى الخطورة:** متوسط (Moderate)
* **الثغرات المرتبطة:**
  * **CVE-2022-45143:** ثغرة تتعلق بخلل في معالجة بعض خوارزميات التشفير وتوقيعات DSA/ECDSA، مما قد يسمح للمهاجمين باستغلالها لتسريب معلومات أو تجاوز التوقيع في سيناريوهات محددة.
  * **CVE-2023-33201:** ثغرة في التحقق من صحة مسارات شهادات X.509 قد تؤدي إلى حدوث استهلاك مفرط للموارد أو تجاوز الذاكرة (Denial of Service).
* **التوصية المستقبلية:** الترقية إلى الإصدار `2.0.0` أو أعلى (أو استخدام الحزمة البديلة `Portable.BouncyCastle`). 
  > [!WARNING]
  > يجب الحذر عند الترقية لأن بعض أسماء الفئات والـ Namespaces قد تتغير في الإصدارات الجديدة، مما يتطلب مراجعة كود التوقيع واختباره بالكامل مع توكين حقيقي.

---

## 2. مكتبة Newtonsoft.Json (الإصدار الحالي: 11.0.1)
تُستخدم المكتبة على نطاق واسع في المشروع لتحويل الفواتير من كائنات برمجية إلى نصوص JSON وبالعكس.

* **مستوى الخطورة:** عالي (High)
* **الثغرات المرتبطة:**
  * **CVE-2022-45688 / GHSA-5crp-9r3c-p9vr:** ثغرة تجاوز مفرط للموارد (Stack Overflow) عند محاولة فك تسلسل (Deserialization) نصوص JSON معقدة أو متداخلة بشكل عميق جداً، مما يتيح للمهاجم إحداث توقف مفاجئ للتطبيق (Crash) عن بعد.
* **التوصية المستقبلية:** الترقية إلى الإصدار `13.0.3` أو أعلى.
  > [!TIP]
  > الترقية إلى الإصدار 13 آمنة بنسبة كبيرة جداً ولا تحتوي على تغييرات جذرية (Breaking Changes) تؤثر على الكود الحالي للمشروع.
